import styled from 'styled-components';

export const LayoutEditorContainer = styled.div`
    .grid-cell {
        &:hover {
            background-color: #34ace0 !important;
            cursor: pointer;
        }
    }
`